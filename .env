# Dev DB (SQLite). Change to Postgres later.
DATABASE_URL=postgresql+psycopg2://postgres:1234@localhost:5432/taskdb?connect_timeout=10&sslmode=prefer

# JWT / auth
JWT_SECRET=change-this-to-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS (tighten later)
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
ENV=dev
