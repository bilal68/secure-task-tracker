# Dev DB (SQLite). Change to Postgres later.
DATABASE_URL=sqlite:///./dev.db

# JWT / auth
JWT_SECRET=change-this-to-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS (tighten later)
CORS_ORIGINS=http://localhost:3000
ENV=dev
